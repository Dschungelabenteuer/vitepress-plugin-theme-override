/** Plugin's log key. */
export const logKey = '[vitepress-plugin-theme-override]';
/** Vitepress' theme folder name. */
export const themeName = 'theme-default';
/** Vitepress' theme path. */
export const themePath = `/vitepress/dist/client/${themeName}/`;
